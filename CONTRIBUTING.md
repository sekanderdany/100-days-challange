# Contributing

To avoid committing large Jupyter Notebook outputs into the repository, install and enable `nbstripout` before working with notebooks:

```bash
pip install nbstripout
nbstripout --install
```
