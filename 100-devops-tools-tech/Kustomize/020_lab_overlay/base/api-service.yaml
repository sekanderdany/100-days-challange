apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: ecommerce
    component: api
spec:
  selector:
    app: ecommerce
    component: api
  ports:
  - port: 80
    targetPort: 80
    name: http
  type: ClusterIP
