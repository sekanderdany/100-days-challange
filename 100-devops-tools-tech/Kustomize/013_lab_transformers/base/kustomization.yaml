apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- api-deployment.yaml
- redis-deployment.yaml
- api-service.yaml
- redis-service.yaml

commonLabels:
  app: microservices
  managed-by: kustomize
