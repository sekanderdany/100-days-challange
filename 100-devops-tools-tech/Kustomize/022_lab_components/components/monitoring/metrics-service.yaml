apiVersion: v1
kind: Service
metadata:
  name: payment-api-metrics
  labels:
    app: payment
    component: api
    monitoring: enabled
spec:
  selector:
    app: payment
    component: api
  ports:
  - port: 9100
    targetPort: 9100
    name: metrics
  type: ClusterIP
